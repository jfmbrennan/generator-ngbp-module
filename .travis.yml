language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ngbp-module' ]; then cd .. && eval "mv $currentfolder generator-ngbp-module" && cd generator-ngbp-module; fi
